[package]
name = "tauri-plugin-polymux-apple"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-polymux-apple"

[dependencies]
tauri = "2"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
