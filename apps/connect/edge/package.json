{
  "name": "@polymux/connect-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && node scripts/check-config.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
