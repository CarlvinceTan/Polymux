name: Site

on:
  push:
    branches: [main, dev]
    paths:
      - "apps/site/**"
      - ".github/workflows/site.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: polymux-site-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            apps/site/package-lock.json

      # The site's share page imports desktop renderer components, so the
      # monorepo root (workspaces, @tsconfig/svelte, the shared packages) must
      # be installed before the site is checked or built.
      - run: npm ci
      - run: npm ci --prefix apps/site
      - run: npm test --prefix apps/site
      - run: npm run check --prefix apps/site
      - run: npm run build --prefix apps/site
